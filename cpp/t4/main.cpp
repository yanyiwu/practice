#include <iostream>
#include <bitset>
#include <vector>
#include <cassert>
using namespace std;

int main()
{
    int staff_size = 27;
    double salaries[staff_size];
    int * a = new(std::nothrow) int[10000000000];
    assert(a);
    //vector<int> a;
    //a.resize(1000000000000);
    return 0;
}
